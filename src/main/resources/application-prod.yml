

logging:
 level:
   org:
    springframework:
     jdbc:
      core: DEBUG
      
spring:
 datasource:
  driver-class-name: com.mysql.cj.jdbc.Driver
  url: jdbc:mysql://app-vacina-db.mysql.database.azure.com:3306/vacinacao?serverTimezone=UTC&useSSL=true&requireSSL=false
  username: appvacinadb@app-vacina-db
  password: "@ppVacinaDb" 
  initialization-mode: always
 jpa:
  hibernate:
   ddl-auto: update
   
management:
  endpoints:
    enabled-by-default: false
  endpoint:
    info:
      enabled: true   
    health:
      enabled: true
    